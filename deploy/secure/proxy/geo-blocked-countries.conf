# GeoIP blocked countries (ISO 3166-1 alpha-2 codes, one per line).
# Lines starting with # are comments.
# Refresh the CIDR blocklist after editing: docker exec pipelock /refresh-geodb
#
# Common sanctions/high-risk countries:
KP
IR
SY
CU
