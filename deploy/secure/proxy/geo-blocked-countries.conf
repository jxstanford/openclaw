# GeoIP blocked countries (ISO 3166-1 alpha-2 codes, one per line).
# Lines starting with # are comments.
# Data source: db-ip.com Lite (CC BY 4.0) — no account required.
# Refresh: docker exec pipelock /usr/local/bin/refresh-geodb
#
# Common sanctions/high-risk countries:
KP
IR
SY
CU
